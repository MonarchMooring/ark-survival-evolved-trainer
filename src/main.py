[
    {
        "path": "ark-survival-evolved-trainer/requirements.txt",
        "content": "pywin32==306\npsutil==5.9.8\npynput==1.7.7\n"
    },
    {
        "path": "ark-survival-evolved-trainer/trainer/memory_reader.py",
        "content": "\"\"\"Low-level memory reader for ARK: Survival Evolved process.\"\"\"\nimport ctypes\nimport ctypes.wintypes\nfrom typing import Optional, List, Tuple\n\nPROCESS_VM_READ = 0x0010\nPROCESS_QUERY_INFORMATION = 0x0400\nkernel32 = ctypes.windll.kernel32\n\n\nclass MemoryReader:\n    \"\"\"Handles reading process memory for ARK trainer.\"\"\"\n\n    def __init__(self, process_name: str = \"ShooterGame.exe\"):\n        self.process_name = process_name\n        self.process_handle: Optional[int] = None\n        self.base_address: Optional[int] = None\n\n    def find_process(self) -> bool:\n        \"\"\"Locate the ARK process by name and open a handle.\"\"\"\n        import psutil\n        for proc in psutil.process_iter([\"pid\", \"name\"]):\n            if proc.info[\"name\"] and proc.info[\"name\"].lower() == self.process_name.lower():\n                pid = proc.info[\"pid\"]\n                self.process_handle = kernel32.OpenProcess(\n                    PROCESS_VM_READ | PROCESS_QUERY_INFORMATION,\n                    False, pid\n                )\n                if self.process_handle:\n                    self.base_address = self._get_base_address(pid)\n                    return True\n        return False\n\n    def _get_base_address(self, pid: int) -> Optional[int]:\n        \"\"\"Retrieve the base address of the main module.\"\"\"\n        import psutil\n        proc = psutil.Process(pid)\n        for mmap in proc.memory_maps(grouped=False):\n            if self.process_name.lower() in mmap.path.lower():\n                return int(mmap.addr.split(\"-\")[0], 16)\n        return None\n\n    def read_int32(self, address: int) -> Optional[int]:\n        \"\"\"Read a 4-byte integer from the process memory.\"\"\"\n        if not self.process_handle:\n            return None\n        buffer = ctypes.c_int32(0)\n        bytes_read = ctypes.c_size_t(0)\n        success = kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            ctypes.byref(buffer),\n            ctypes.sizeof(buffer),\n            ctypes.byref(bytes_read)\n        )\n        return buffer.value if success else None\n\n    def read_float(self, address: int) -> Optional[float]:\n        \"\"\"Read a 4-byte float from the process memory.\"\"\"\n        if not self.process_handle:\n            return None\n        buffer = ctypes.c_float(0.0)\n        bytes_read = ctypes.c_size_t(0)\n        success = kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            ctypes.byref(buffer),\n            ctypes.sizeof(buffer),\n            ctypes.byref(bytes_read)\n        )\n        return buffer.value if success else None\n\n    def read_bytes(self, address: int, size: int) -> Optional[bytes]:\n        \"\"\"Read raw bytes from process memory.\"\"\"\n        if not self.process_handle:\n            return None\n        buffer = ctypes.create_string_buffer(size)\n        bytes_read = ctypes.c_size_t(0)\n        success = kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            buffer,\n            size,\n            ctypes.byref(bytes_read)\n        )\n        return buffer.raw[:bytes_read.value] if success else None\n\n    def close(self) -> None:\n        \"\"\"Close the process handle.\"\"\"\n        if self.process_handle:\n            kernel32.CloseHandle(self.process_handle)\n            self.process_handle = None\n"
    },
    {
        "path": "ark-survival-evolved-trainer/trainer/offsets.py",
        "content": "\"\"\"Memory offsets for ARK: Survival Evolved game version 357.2.\"\"\"\n\n# Base pointer chain to local player object\nLOCAL_PLAYER_PTR = 0x04F1A2B0  # Example offset, would be found via pattern scanning\nLOCAL_PLAYER_OFFSETS = [0x0, 0x28, 0x4C0]  # Dereference chain\n\n# Stat offsets relative to player object\nHEALTH_OFFSET = 0x0A0  # Float\nSTAMINA_OFFSET = 0x0A8  # Float\nOXYGEN_OFFSET = 0x0B0  # Float\nFOOD_OFFSET = 0x0B8  # Float\nWATER_OFFSET = 0x0C0  # Float\nWEIGHT_OFFSET = 0x0C8  # Float\n\n# Experience and level\nEXPERIENCE_OFFSET = 0x0E0  # Float\nLEVEL_OFFSET = 0x0E8  # Int32\n\n# Engram points\nENGRAM_POINTS_OFFSET = 0x0F0  # Int32\n\n# Inventory related\nINVENTORY_PTR_OFFSET = 0x1A0  # Pointer to inventory object\nINVENTORY_ITEM_COUNT_OFFSET = 0x10  # Int32 (relative to inventory)\nINVENTORY_ITEMS_ARRAY_OFFSET = 0x18  # Pointer to array of item pointers\n\n# Item structure offsets (relative to each item pointer)\nITEM_ID_OFFSET = 0x08  # Int32\nITEM_QUANTITY_OFFSET = 0x20  # Int32\nITEM_QUALITY_OFFSET = 0x28  # Float\n\n# Game time / day cycle\nGAME_TIME_OFFSET = 0x04F2B100  # Float (seconds since midnight)\n"
    },
    {
        "path": "ark-survival-evolved-trainer/trainer/trainer.py",
        "content": "\"\"\"Main trainer class for ARK: Survival Evolved.\"\"\"\nimport time\nfrom typing import Optional, Callable\nfrom .memory_reader import MemoryReader\nfrom . import offsets\n\n\nclass ARKTrainer:\n    \"\"\"Provides cheat functionality for ARK: Survival Evolved.\"\"\"\n\n    def __init__(self):\n        self.reader = MemoryReader()\n        self._connected = False\n        self._running = False\n        self._callbacks: list[Callable] = []\n\n    def connect(self) -> bool:\n        \"\"\"Connect to the ARK process.\"\"\"\n        self._connected = self.reader.find_process()\n        return self._connected\n\n    @property\n    def is_connected(self) -> bool:\n        return self._connected\n\n    def _resolve_pointer_chain(self, base_ptr: int, offsets_list: list[int]) -> Optional[int]:\n        \"\"\"Dereference a pointer chain to get the final address.\"\"\"\n        addr = base_ptr\n        for offset in offsets_list:\n            ptr = self.reader.read_int32(addr + offset)\n            if ptr is None:\n                return None\n            addr = ptr\n        return addr\n\n    def get_local_player_address(self) -> Optional[int]:\n        \"\"\"Get the address of the local player object.\"\"\"\n        if not self._connected:\n            return None\n        base = self.reader.read_int32(offsets.LOCAL_PLAYER_PTR)\n        if base is None:\n            return None\n        return self._resolve_pointer_chain(base, offsets.LOCAL_PLAYER_OFFSETS)\n\n    def get_health(self) -> Optional[float]:\n        \"\"\"Get current health value.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        return self.reader.read_float(player_addr + offsets.HEALTH_OFFSET)\n\n    def get_stamina(self) -> Optional[float]:\n        \"\"\"Get current stamina value.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        return self.reader.read_float(player_addr + offsets.STAMINA_OFFSET)\n\n    def get_food(self) -> Optional[float]:\n        \"\"\"Get current food value.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        return self.reader.read_float(player_addr + offsets.FOOD_OFFSET)\n\n    def get_water(self) -> Optional[float]:\n        \"\"\"Get current water value.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        return self.reader.read_float(player_addr + offsets.WATER_OFFSET)\n\n    def get_level(self) -> Optional[int]:\n        \"\"\"Get player level.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        return self.reader.read_int32(player_addr + offsets.LEVEL_OFFSET)\n\n    def get_experience(self) -> Optional[float]:\n        \"\"\"Get current experience points.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        return self.reader.read_float(player_addr + offsets.EXPERIENCE_OFFSET)\n\n    def get_engram_points(self) -> Optional[int]:\n        \"\"\"Get available engram points.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        return self.reader.read_int32(player_addr + offsets.ENGRAM_POINTS_OFFSET)\n\n    def get_inventory_items(self) -> Optional[list[dict]]:\n        \"\"\"Get list of items in player inventory.\"\"\"\n        player_addr = self.get_local_player_address()\n        if player_addr is None:\n            return None\n        inv_ptr = self.reader.read_int32(player_addr + offsets.INVENTORY_PTR_OFFSET)\n        if inv_ptr is None:\n            return None\n        item_count = self.reader.read_int32(inv_ptr + offsets.INVENTORY_ITEM_COUNT_OFFSET)\n        if item_count is None or item_count <= 0:\n            return []\n        items_array_ptr = self.reader.read_int32(inv_ptr + offsets.INVENTORY_ITEMS_ARRAY_OFFSET)\n        if items_array_ptr is None:\n            return []\n        items = []\n        for i in range(item_count):\n            item_ptr = self.reader.read_int32(items_array_ptr + i * 4)\n            if item_ptr is None:\n                continue\n            item_id = self.reader.read_int32(item_ptr + offsets.ITEM_ID_OFFSET)\n            quantity = self.reader.read_int32(item_ptr + offsets.ITEM_QUANTITY_OFFSET)\n            quality = self.reader.read_float(item_ptr + offsets.ITEM_QUALITY_OFFSET)\n            items.append({\n                \"id\": item_id,\n                \"quantity\": quantity,\n                \"quality\": quality\n            })\n        return items\n\n    def start_monitoring(self, interval: float = 1.0) -> None:\n        \"\"\"Start periodic stat monitoring in a background thread.\"\"\"\n        import threading\n        self._running = True\n        def _monitor():\n            while self._running:\n                if self._connected:\n                    stats = {\n                        \"health\": self.get_health(),\n                        \"stamina\": self.get_stamina(),\n                        \"food\": self.get_food(),\n                        \"water\": self.get_water(),\n                        \"level\": self.get_level(),\n                        \"exp\": self.get_experience(),\n                        \"engram_points\": self.get_engram_points()\n                    }\n                    for cb in self._callbacks:\n                        try:\n                            cb(stats)\n                        except Exception:\n                            pass\n                time.sleep(interval)\n        thread = threading.Thread(target=_monitor, daemon=True)\n        thread.start()\n\n    def stop_monitoring(self) -> None:\n        \"\"\"Stop the monitoring thread.\"\"\"\n        self._running = False\n\n    def register_callback(self, callback: Callable) -> None:\n        \"\"\"Register a callback for stat updates.\"\"\"\n        self._callbacks.append(callback)\n\n    def disconnect(self) -> None:\n        \"\"\"Disconnect from the process.\"\"\"\n        self.stop_monitoring()\n        self.reader.close()\n        self._connected = False\n"
    },
    {
        "path": "ark-survival-evolved-trainer/trainer/__init__.py",
        "content": "\"\"\"ARK: Survival Evolved Trainer package.\"\"\"\nfrom .trainer import ARKTrainer\nfrom .memory_reader import MemoryReader\n\n__version__ = \"1.0.0\"\n__all__ = [\"ARKTrainer\", \"MemoryReader\"]\n"
    },
    {
        "path": "ark-survival-evolved-trainer/main.py",
        "content": "\"\"\"Entry point for the ARK: Survival Evolved Trainer CLI.\"\"\"\nimport sys\nimport time\nfrom trainer import ARKTrainer\n\n\ndef print_stats(stats: dict) -> None:\n    \"\"\"Print player stats to console.\"\"\"\n    print(\"\\033[H\\033[J\", end=\"\")  # Clear terminal\n    print(\"=== ARK: Survival Evolved Trainer ===\")\n    print(f\"Health: {stats.get('health', 'N/A')}\")\n    print(f\"Stamina: {stats.get('stamina', 'N/A')}\")\n    print(f\"Food: {stats.get('food', 'N/A')}\")\n    print(f\"Water: {stats.get('water', 'N/A')}\")\n    print(f\"Level: {stats.get('level', 'N/A')}\")\n    print(f\"Experience: {stats.get('exp', 'N/A')}\")\n    print(f\"Engram Points: {stats.get('engram_points', 'N/A')}\")\n    print(\"\\nPress Ctrl+C to exit.\")\n\n\ndef main() -> None:\n    \"\"\"Main function to run the trainer.\"\"\"\n    trainer = ARKTrainer()\n    print(\"Connecting to ARK: Survival Evolved...\")\n    if not trainer.connect():\n        print(\"Error: Could not find ARK process. Make sure the game is running.\")\n        sys.exit(1)\n    print(\"Connected successfully.\")\n    trainer.register_callback(print_stats)\n    trainer.start_monitoring(interval=0.5)\n    try:\n        while True:\n            time.sleep(1)\n    except KeyboardInterrupt:\n        print(\"\\nShutting down...\")\n    finally:\n        trainer.disconnect()\n\n\nif __name__ == \"__main__\":\n    main()\n"
    },
    {
        "path": "ark-survival-evolved-trainer/tests/test_trainer.py",
        "content": "\"\"\"Unit tests for the ARK Trainer module.\"\"\"\nimport unittest\nfrom unittest.mock import patch, MagicMock, PropertyMock\nfrom trainer import ARKTrainer\nfrom trainer.memory_reader import MemoryReader\n\n\nclass TestARKTrainer(unittest.TestCase):\n    \"\"\"Test cases for ARKTrainer class.\"\"\"\n\n    def setUp(self):\n        \"\"\"Set up test fixtures.\"\"\"\n        self.trainer = ARKTrainer()\n\n    def test_initial_state(self):\n        \"\"\"Test initial state of trainer.\"\"\"\n        self.assertFalse(self.trainer.is_connected)\n        self.assertIsNotNone(self.trainer.reader)\n        self.assertIsInstance(self.trainer.reader, MemoryReader)\n\n    @patch('trainer.trainer.MemoryReader.find_process')\n    def test_connect_success(self, mock_find):\n        \"\"\"Test successful connection to process.\"\"\"\n        mock_find.return_value = True\n        result = self.trainer.connect()\n        self.assertTrue(result)\n        self.assertTrue(self.trainer.is_connected)\n\n    @patch('trainer.trainer.MemoryReader.find_process')\n    def test_connect_failure(self, mock_find):\n        \"\"\"Test failed connection to process.\"\"\"\n        mock_find.return_value = False\n        result = self.trainer.connect()\n        self.assertFalse(result)\n        self.assertFalse(self.trainer.is_connected)\n\n    @patch('trainer.trainer.ARKTrainer.get_local_player_address')\n    def test_get_health(self, mock_addr):\n        \"\"\"Test retrieving health value.\"\"\"\n        mock_addr.return_value = 0x12345678\n
